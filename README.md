# openrgb-idle-controller
A Python script for dynamic lighting control. It automatically turns off the LED strips when the system is idle (via WinAPI) and launches a smooth welcome animation when the system wakes up
